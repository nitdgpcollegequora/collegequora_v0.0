<!DOCTYPE html>
<html lang="en">

    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>College Quora</title>
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/css/bootstrap.min.css">
        <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css">
        <style>
            #regform {
                width: 50%;
            }

            @media screen and (max-width:768px) {
                #regform {
                    width: 95%;
                }
            }
        </style>
    </head>

    <body>
        <div class="container-md container-fluid">
            <div class="w-100 text-center mt-3">
                <h2 class="font-weight-bold">Register</h2>
            </div>
            <%if(error){%>
              <%error.forEach(function(err){%>
                <div class="alert alert-danger text-center">
                  <%=err%>
                </div>
              <%})%>
            <%}%>
            <form method="POST" action="/register" class="m-auto" id="regform">
                <div class="form-group">

                    <label for="username" class="font-weight-bold">Enter a Username</label>
                    <input type="text" class="form-control" name="username" placeholder="Username">
                    <!-- <% if(messages.length >= 1){ %>
                        <div class="alert alert-danger" role="alert"><%= messages[0].message %></div>
                    <% } %> -->
                </div>
                <div class="form-group">
                    <label for="email" class="font-weight-bold">E-Mail</label>
                    <input type="email" class="form-control" name="email" placeholder="Email">
                    <!-- <% if(messages.length >= 2 && messages[1].fail){ %>
                        <div class="alert alert-danger" role="alert"><%= messages[1].message %></div>
                    <% } %> -->
                </div>
                <div class="form-group">
                    <label for="password" class="font-weight-bold"> Set Password</label>
                    <input type="password" class="form-control" name="password" placeholder="password">
                    <!-- <% if(messages.length >= 3 && messages[2].fail){ %>
                        <div class="alert alert-danger" role="alert"><%= messages[2].message %></div>
                    <% } %> -->
                </div>
                <div class="form-group">
                    <button type="submit" class="mb-3 btn btn-primary font-weight-bold">
                        Register
                    </button>
                    <a href="/login" style="text-decoration: none; float: right;" class="mr-2">Login</a>
                </div>
            </form>
        </div>
    </body>

</html>
